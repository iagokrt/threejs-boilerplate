// uniform vec3 uColor;
// uniform vec2 uMouse;
// uniform vec2 uResolution;

void main() {
    // vec3 color = vec3(uMouse.x / uResolution.x, 0.0, uMouse.y / uResolution.y);
    gl_FragColor = vec4(1.0, 1.0, 0.0, 1.0);
}